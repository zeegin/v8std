###### #std464

# Обработчик события ПередЗаписью

###### 1.

В обработчике `#!bsl ПередЗаписью` выполняйте:

- заполнение реквизитов объекта (набора записей, значения константы);
- проверку правильности заполнения;
- проверки связанности состояния объекта с внешними данными;
- работу со «старыми» значениями реквизитов, уже сохраненными в базе.

Этот обработчик особенно полезен при редактировании уже записанных объектов.

###### 2.

Все действия в обработчике `#!bsl ПередЗаписью` выполняйте только после проверки `#!bsl ОбменДанными.Загрузка`.

!!! example "Пример"

    ```bsl
    Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
    
        Если ОбменДанными.Загрузка Тогда
            Возврат;
        КонецЕсли;

        // Предпроверки и дозаполнение реквизитов объекта.
        ...
    КонецПроцедуры
    ```

<!-- diagnostic-backlinks:start clause=2 -->
###### Проверки

~[#acc:75](../diagnostics/acc/75.md)~
~[#bslls:DataExchangeLoading](../diagnostics/bslls/DataExchangeLoading.md)~
~[#v8cs:data-exchange-load](../diagnostics/v8-code-style/data-exchange-load.md)~
<!-- diagnostic-backlinks:end clause=2 -->

###### См. также

- [#std773: Использование признака ОбменДанными.Загрузка в обработчиках событий объекта](773.md).
- [#std463: Проверки, выполняемые в и вне транзакции записи объекта](463.md)

###### Источник

https://its.1c.ru/db/v8std#content:464
