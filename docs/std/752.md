###### #std752

# Обработчик события ПередУдалением

###### 1.

В обработчике `#!bsl ПередУдалением` модуля объекта выполняйте действия, которые должны быть сделаны до удаления объекта.

Например, перед удалением присоединенного файла может потребоваться очистить ссылки на этот файл в объекте-владельце.

!!! quote "В стандарте не указано"

    В некоторых случаях удаление связанных данных выполняется автоматически.
    Например, когда ссылка на удаляемый объект является ведущим измерением.

###### 2.

Все действия в обработчике `#!bsl ПередУдалением` выполняйте только после проверки `#!bsl ОбменДанными.Загрузка`.

При удалении через механизм обмена данными код обработчика не должен выполняться, так как это может приводить к ошибкам.
Например, к обращению к предопределенным объектам после очистки области данных.

!!! example "Пример"

    ```bsl
    Процедура ПередУдалением(Отказ)

        Если ОбменДанными.Загрузка Тогда
            Возврат;
        КонецЕсли;

        // Действия перед удалением объекта.
        ОчиститьСсылкиНаУдаляемыйФайл();

    КонецПроцедуры
    ```

<!-- diagnostic-backlinks:start clause=2 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/acc/75.md">acc:75</a>
<a class="diagnostic-chip" href="../diagnostics/bslls/DataExchangeLoading.md">bslls:DataExchangeLoading</a>
<a class="diagnostic-chip" href="../diagnostics/v8-code-style/data-exchange-load.md">v8cs:data-exchange-load</a>
</div>
<!-- diagnostic-backlinks:end clause=2 -->

###### См. также

- [#std465: Обработчик события ПриЗаписи](465.md)
- [#std773: Использование признака ОбменДанными.Загрузка в обработчиках событий объекта](773.md)

###### Источники

- [Русская версия — ИТС](https://its.1c.ru/db/v8std#content:752)
- [English version — 1Ci Knowledge Base](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_Development_Standards/Creating_and_modifying_metadata_objects/Object_event_handlers/Event_handlers_____BeforeDelete/?language=en)
