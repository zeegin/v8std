---
llms:
  ignore: true
---

###### #std741

# Открытие параметризированных форм


###### 1.

Если форма требует параметров при открытии и предназначена только для программного открытия, не назначайте ее основной формой объекта.
Такая форма не должна открываться из меню «Все функции».

###### 2.

Если у объекта нет других форм, назначайте основной параметризированную форму.
В обработчике `#!bsl ПриСозданииНаСервере` проверяйте параметры формы и при их отсутствии вызывайте исключение.
Текст исключения должен объяснять пользователю, почему форму нельзя открыть.

###### 3.

Объявляйте параметры формы на закладке «Параметры» редактора формы.
Тогда в `#!bsl ПриСозданииНаСервере` не нужно проверять наличие свойств у структуры `#!bsl Параметры`, а состав параметров виден явно.

!!! failure "Неправильно"

    ```bsl
    &НаСервере
    Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
        ФамилияИмяОтчество = Неопределено;
        Если Параметры.Свойство("ФамилияИмяОтчество", ФамилияИмяОтчество) Тогда
            Объект.Наименование = ФамилияИмяОтчество;
        КонецЕсли;
    КонецПроцедуры
    ```

!!! success "Правильно"

    ```bsl
    &НаСервере
    Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
        Объект.Наименование = Параметры.ФамилияИмяОтчество;
    КонецПроцедуры
    ```

<!-- diagnostic-backlinks:start clause=3 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/v8-code-style/optional-form-parameter-access.md">v8cs:optional-form-parameter-access</a>
<a class="diagnostic-chip" href="../diagnostics/v8-code-style/unknown-form-parameter-access.md">v8cs:unknown-form-parameter-access</a>
</div>
<!-- diagnostic-backlinks:end clause=3 -->

<!-- diagnostic-backlinks:start clause=std741 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/acc/1410.md">acc:1410</a>
</div>
<!-- diagnostic-backlinks:end clause=std741 -->

###### Источники

- [Русская версия — ИТС](https://its.1c.ru/db/v8std#content:741)
- [English version — 1Ci Knowledge Base](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_Development_Standards/Designing_user_interfaces/Implementation_of_forms/Opening_parameterized_forms/?language=en)
