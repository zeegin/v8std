###### #std465

# Обработчик события ПриЗаписи

###### 1.

В обработчике `#!bsl ПриЗаписи` модуля объекта (набора записей, значения константы) выполняйте действия, связанные с уже записанным объектом:

- запись связанных данных в другие объекты конфигурации;
- другие действия после изменения объекта.

Не изменяйте данные самого записываемого объекта.
К моменту вызова `#!bsl ПриЗаписи` объект уже записан в базе данных.

###### 2.

Все действия в обработчике `#!bsl ПриЗаписи` выполняйте только после проверки `#!bsl ОбменДанными.Загрузка`.

!!! example "Пример"

    ```bsl
    Процедура ПриЗаписи(Отказ)

        Если ОбменДанными.Загрузка Тогда
            Возврат;
        КонецЕсли;

        // Постобработка после записи объекта.
        ОбновитьСвязанныеДанные();

    КонецПроцедуры
    ```

###### См. также

- [#std463: Проверки, выполняемые в и вне транзакции записи объекта](463.md)
- [#std773: Использование признака ОбменДанными.Загрузка в обработчиках событий объекта](773.md)

###### Проверки
~[#bslls:DataExchangeLoading](../diagnostics/bslls/DataExchangeLoading.md)~
~[#v8cs:data-exchange-load](../diagnostics/v8-code-style/data-exchange-load.md)~

~[#acc:75](../diagnostics/acc/75.md)~
###### Источник

https://its.1c.ru/db/v8std#content:465
