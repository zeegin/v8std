---
llms:
  ignore: true
---

###### #std501

# Общие интерфейсы

###### 1.

Для отображения пунктов меню
и панелей инструментов,
общих для всех
переключаемых прикладных интерфейсов,
в конфигурации создается интерфейс `Общий`.

Для него:

- снимается признак `Переключаемый`;
- в качестве подсистемы
  указывается вся конфигурация.

<!-- diagnostic-backlinks:start clause=1 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/acc/1133.md">acc:1133</a>
<a class="diagnostic-chip" href="../diagnostics/acc/242.md">acc:242</a>
</div>
<!-- diagnostic-backlinks:end clause=1 -->

###### 2.

На верхнем уровне главного меню
обязательно должны располагаться:

- в крайней левой позиции: `Файл`;
- в крайней правой позиции:
  набор подменю `Сервис`, `Окна`, `Справка`.

###### 3.

В меню `Сервис`
может присутствовать подменю
`Переключить интерфейс`
для переключения текущего интерфейса
на другой.

Это подменю должно содержать
список всех переключаемых интерфейсов
конфигурации.

###### 4.

В интерфейсе `Общий`
рекомендуется отключать подменю `Операции`,
кроме случаев,
когда это подменю
используется во всех
переключаемых интерфейсах конфигурации.

###### 5.

В конфигурации
можно создать несколько
непереключаемых интерфейсов,
чтобы реализовать зависимость
состава главного меню
и панелей инструментов
от роли пользователя.

Для этого:

- в состав интерфейсов
  добавляются интерфейсы,
  названия которых
  начинаются со слова `Общий...`;
- у таких интерфейсов
  снимается признак `Переключаемый`;
- право использования
  выдается соответствующим ролям.

###### Источник

https://its.1c.ru/db/v8std#content:501
