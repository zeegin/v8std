###### #std441

# Общие требования к построению конструкций встроенного языка

###### 1.

Ключевые слова встроенного языка пишите канонически, как в документации и Синтакс-помощнике.

!!! failure "Неправильно"

    ```bsl
    конецЕсли
    КОНЕЦЕСЛИ
    конецесли
    Конецесли
    ```

!!! success "Правильно"

    ```bsl
    КонецЕсли
    ```

<!-- diagnostic-backlinks:start clause=1 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/acc/1248.md">acc:1248</a>
<a class="diagnostic-chip" href="../diagnostics/bslls/CanonicalSpellingKeywords.md">bslls:CanonicalSpellingKeywords</a>
<a class="diagnostic-chip" href="../diagnostics/v8-code-style/bsl-canonical-pragma.md">v8cs:bsl-canonical-pragma</a>
</div>
<!-- diagnostic-backlinks:end clause=1 -->

<!-- fix-backlinks:start clause=1 -->
<div class="fix-links" aria-label="Исправления">
<a class="diagnostic-chip diagnostic-chip--fix" href="../diagnostics/autoformat/index.md">autoformat</a>
</div>
<!-- fix-backlinks:end clause=1 -->

###### 2.

Когда подряд идут несколько присваиваний, допускается локально выравнивать `#!bsl =` для лучшей читаемости.

```bsl
ДиалогВыбора.ПолноеИмяФайла = ИмяФайла;
ДиалогВыбора.Каталог        = ИмяПути;
ДиалогВыбора.Заголовок      = НСтр("ru = 'Выберите файл со списком запросов'");
ДиалогВыбора.Фильтр         = НСтр("ru = 'Файлы запросов (*.sel)|*.sel|Все файлы (*.*)|*.*'");
ДиалогВыбора.Расширение     = "sel";
```

Не выравнивайте операторы таким образом по всему модулю.
Делайте это только для соседних строк, где это действительно улучшает чтение.

###### 3.

Составные логические выражения в `#!bsl Если ... КонецЕсли` переносите по [#std444: правилам переноса выражений](444.md).

###### 4.

Логические выражения и значения типа `Булево` не сравнивайте с литералами `#!bsl Истина` и `#!bsl Ложь`.

!!! failure "Неправильно"

    ```bsl
    Если ЭтоНовый() = Истина Тогда
    ```

!!! success "Правильно"

    ```bsl
    Если ЭтоНовый() Тогда
    ```

<!-- diagnostic-backlinks:start clause=4 -->
<div class="diagnostic-links" aria-label="Проверки">
<a class="diagnostic-chip" href="../diagnostics/acc/297.md">acc:297</a>
<a class="diagnostic-chip" href="../diagnostics/bslls/CompareWithBoolean.md">bslls:CompareWithBoolean</a>
</div>
<!-- diagnostic-backlinks:end clause=4 -->

###### 5.

Если нужно сравнить результат выражения, сначала присвойте его промежуточной переменной, а затем сравнивайте переменную.

!!! failure "Неправильно"

    ```bsl
    Если Вопрос(НСтр("ru = 'Данные еще не записаны. Записать?'"), РежимДиалогаВопрос.ДаНет,, КодВозвратаДиалога.Да) = КодВозвратаДиалога.Да Тогда
      Записать();
    Иначе
      Возврат;
    КонецЕсли;
    ```

!!! success "Правильно"

    ```bsl
    Ответ = Вопрос(НСтр("ru = 'Данные еще не записаны. Записать?'"), РежимДиалогаВопрос.ДаНет,, КодВозвратаДиалога.Да);
    Если Ответ = КодВозвратаДиалога.Да Тогда
      Записать();
    Иначе
      Возврат;
    КонецЕсли;
    ```

###### 6.

Используйте системные наборы значений везде, где это возможно.
Например, вместо `#!bsl Символ(10)` используйте `#!bsl Символы.ПС`.

###### См. также

- [#std444: Перенос выражений](444.md)
- [#std456: Тексты модулей](456.md)

###### Источники

- [Русская версия — ИТС](https://its.1c.ru/db/v8std#content:441)
- [English version — 1Ci Knowledge Base](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_Development_Standards/Code_conventions/Using_1C_Enterprise_language_structures/General_1C_Enterprise_application_development_practices/?language=en)
