###### #std451

# Программное создание прикладных объектов


###### 1.

Для программного создания прикладных объектов используйте методы соответствующих менеджеров (`#!bsl СоздатьЭлемент()`, `#!bsl СоздатьДокумент()`, `#!bsl СоздатьНаборЗаписей()` и т.п.).

Если для объекта есть менеджер, использование конструктора `#!bsl Новый` запрещено.

!!! failure "Неправильно"

    ```bsl
    ДокументПриходная = Новый("ДокументОбъект.ПоступлениеТоваровУслуг");
    ```

!!! success "Правильно"

    ```bsl
    ДокументПриходная = Документы.ПоступлениеТоваровУслуг.СоздатьДокумент();
    ```

<!-- diagnostic-backlinks:start clause=1 -->
###### Проверки

~[#acc:99](../diagnostics/acc/99.md)~
<!-- diagnostic-backlinks:end clause=1 -->

###### 2.

При программном создании объекта явно вызывайте метод `#!bsl Заполнить()`.
Если данных для заполнения нет, передавайте `#!bsl Неопределено`.

Тогда корректно сработают:

- свойства реквизитов объекта `Значение заполнения`;
- обработчик `#!bsl ОбработкаЗаполнения`;
- подписки на событие заполнения.

Поведение будет таким же, как при интерактивной работе с объектом.

!!! failure "Неправильно"

    ```bsl
    Папка = Справочники.ПапкиФайлов.СоздатьЭлемент();
    // ...
    Папка.Записать();
    ```

!!! success "Правильно"

    ```bsl
    Папка = Справочники.ПапкиФайлов.СоздатьЭлемент();
    // ...
    Папка.Заполнить(Неопределено);
    // ...
    Папка.Записать();
    ```

Исключение возможно, когда объект полностью загружается из внешнего источника при обмене данными или при восстановлении базы из резервной копии (загрузка из XML).

<!-- diagnostic-backlinks:start clause=std451 -->
###### Проверки

~[#acc:1444](../diagnostics/acc/1444.md)~
<!-- diagnostic-backlinks:end clause=std451 -->

###### Источники

- [Русская версия — ИТС](https://its.1c.ru/db/v8std#content:451)
- [English version — 1Ci Knowledge Base](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_Development_Standards/Code_conventions/Using_applied_objects_and_universal_value_collections/Creating_applied_objects_programmatically/?language=en)
